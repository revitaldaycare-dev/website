# Local tooling (codegraph index symlink + OpenCode plans) - not part of the site
.codegraph
.omo/

# macOS
.DS_Store

# Local Tailwind build tooling (rebuilt on demand; styles.css is committed)
node_modules/
